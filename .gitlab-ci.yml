image: trion/ng-cli

cache:
  untracked: true
  paths:
    - node_modules/

stages:
  - build
  - deploy
  

build:
  stage: build
  script:
    - npm install
    - ng build --prod
  only:
    - master
  tags:
    - nodeJS

deploy:
  stage: deploy
  script:
    - npm i -g netlify-cli
    - netlify deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod
  dependencies:
    - build
  only:
    - master
  tags:
    - nodeJS
